mhartid（Hard ID register）硬件ID寄存器

mhartid是一个MXLEN bit的只读寄存器，保存处理器当前正运行时软件的线程ID信息，
处理器必须实现mhartid的读取功能，在多核处理器中hardid不一定是连续编号的，
但至少有一个hard的hardid是0，同时保证运行环境中hardid的互不相同。



BSS段通常是指用来存放程序中未初始化的或者初始化为0的全局变量和静态变量的一块内存区域。
特点是可读写的，在程序执行之前BSS段会自动清0。
